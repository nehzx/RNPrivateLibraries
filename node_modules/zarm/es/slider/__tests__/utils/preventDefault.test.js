import preventDefault from '../../utils/preventDefault';
describe('preventDefault', function () {
  it('should pass', function () {
    var mEvent = {
      preventDefault: jest.fn()
    };
    preventDefault(mEvent);
    expect(mEvent.preventDefault).toBeCalledTimes(1);
  });
});