declare const parseState: (props: {
    min?: Date;
    max?: Date;
    value?: Date | Date[];
    defaultValue?: Date | Date[];
    multiple: boolean;
}) => {
    value: Date[];
    min: Date;
    max: Date;
    startMonth: Date;
    endMonth: Date;
    refresh: boolean;
    steps: number;
    multiple: boolean;
};
export default parseState;
