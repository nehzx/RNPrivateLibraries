export interface IFileProps {
    file: File;
    quality?: number;
    fileType: string;
    maxWidth?: number | string;
    maxHeight?: number | string;
}
/**
 * generate preview image
 * modify image quality
 *
 * @param file
 * @param quality
 * @param fileType
 * @param maxWidth
 * @param maxHeight
 * @param callback
 */
export default function createThumbnail({ file, quality, fileType, maxWidth, maxHeight, }: IFileProps): Promise<string>;
