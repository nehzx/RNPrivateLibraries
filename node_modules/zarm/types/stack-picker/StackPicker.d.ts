import React from 'react';
import PropsType from './PropsType';
export interface StackPickerProps extends PropsType {
    prefixCls?: string;
    className?: string;
}
declare const _default: React.MemoExoticComponent<({ prefixCls, className, dataSource, defaultValue, value, displayMember, valueMember, title, visible, cols, labelAddon, displayRender, itemRender, onChangeValidate, cancelText, okText, onChange, onOk, onCancel, maskClosable, locale, }: StackPickerProps) => JSX.Element>;
export default _default;
